global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8888'] # Collector's own metrics
  - job_name: 'backend'
    static_configs:
      - targets: ['otel-collector:8889'] # Metrics flowing through collector
